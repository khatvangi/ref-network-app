# nginx config for refnet.thebeackers.com
# copy to: /etc/nginx/conf.d/refnet.conf
# then: sudo nginx -t && sudo systemctl reload nginx

server {
    listen 80;
    server_name refnet.thebeackers.com;

    # redirect HTTP to HTTPS (uncomment after certbot setup)
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://127.0.0.1:8765;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 300s;  # long timeout for analysis jobs
    }
}

# HTTPS config (uncomment after running certbot)
# server {
#     listen 443 ssl http2;
#     server_name refnet.thebeackers.com;
#
#     ssl_certificate /etc/letsencrypt/live/refnet.thebeackers.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/refnet.thebeackers.com/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:8765;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#         proxy_read_timeout 300s;
#     }
# }
